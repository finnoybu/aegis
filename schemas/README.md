← [Back to README](../README.md)

# Schemas

This directory holds **machine-readable schemas** used by Aegis.

In V0, the canonical schemas live in the repository root
(e.g. `AEGIS_ACTION_SCHEMA.json`).

Schemas may be relocated here in later phases if they require
independent versioning or tooling.
